--- a/library/std/src/sys/net/connection/wasip1.rs	2026-04-30 21:59:46
+++ b/library/std/src/sys/net/connection/wasip1.rs	2026-04-30 22:26:48
@@ -72,0 +73,2 @@
+const YURT_IPPROTO_TCP: i32 = 6;
+const YURT_TCP_NODELAY: i32 = 1;
@@ -110,0 +113 @@
+    fn setsockopt(fd: i32, level: i32, optname: i32, optval: *const c_int, optlen: YurtSockLen) -> i32;
@@ -135,0 +139,14 @@
+}
+
+fn tcp_set_nodelay(fd: RawFd, enabled: bool) -> io::Result<()> {
+    let value: c_int = if enabled { 1 } else { 0 };
+    let rc = unsafe {
+        setsockopt(
+            fd as i32,
+            YURT_IPPROTO_TCP,
+            YURT_TCP_NODELAY,
+            &value,
+            mem::size_of::<c_int>() as YurtSockLen,
+        )
+    };
+    if rc != 0 { Err(io::Error::last_os_error()) } else { Ok(()) }
@@ -137,0 +155,9 @@
+fn tcp_nodelay(fd: RawFd) -> io::Result<bool> {
+    let mut value: c_int = 0;
+    let mut len = mem::size_of::<c_int>() as YurtSockLen;
+    let rc = unsafe {
+        getsockopt(fd as i32, YURT_IPPROTO_TCP, YURT_TCP_NODELAY, &mut value, &mut len)
+    };
+    if rc != 0 { Err(io::Error::last_os_error()) } else { Ok(value != 0) }
+}
+
@@ -294,2 +320,2 @@
-    pub fn set_nodelay(&self, _: bool) -> io::Result<()> {
-        unsupported()
+    pub fn set_nodelay(&self, value: bool) -> io::Result<()> {
+        tcp_set_nodelay(self.socket().as_raw_fd(), value)
@@ -299 +325 @@
-        unsupported()
+        tcp_nodelay(self.socket().as_raw_fd())
