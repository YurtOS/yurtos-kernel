[package]
name = "yurt-guest-compat"
version = "0.1.0"
edition = "2021"
publish = false
description = "Safe Rust wrappers over the yurt guest compatibility runtime Tier 1 ABI."

[lib]
path = "src/lib.rs"

[dependencies]
yurt-guest-compat-sys = { path = "../yurt-guest-compat-sys" }
libc = "0.2"

[dev-dependencies]
