[package]
name = "yurt-guest-compat-sys"
version = "0.1.0"
edition = "2021"
publish = false
description = "Native bindings to libyurt_guest_compat.a; emits link directives unless cargo-yurt has already injected them."
links = "yurt_guest_compat"

[lib]
path = "src/lib.rs"

[build-dependencies]

[dev-dependencies]
tempfile = "3"
