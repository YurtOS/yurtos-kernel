[package]
name = "yurt-microkernel-wasmer"
version = "0.1.0"
edition = "2021"
description = "Wasmer-backed microkernel — STUB. Validates the WasmEngine trait shape; real impl pulls wasmer."

[lib]
name = "yurt_microkernel_wasmer"
path = "src/lib.rs"

[dependencies]
yurt-microkernel-core = { path = "../microkernel-core" }
thiserror = "2"
